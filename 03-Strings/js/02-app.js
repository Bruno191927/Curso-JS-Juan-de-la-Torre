const producto = "Monitor 20";
console.log(producto.length);//contar el numero de caracteres

// nos da la posicion donde se encuentra la palabra y si no la encuentra da -1
console.log(producto.indexOf('Monitor'));

//devuelve true o false si hay la palabras
console.log(producto.includes('Monitor')); 