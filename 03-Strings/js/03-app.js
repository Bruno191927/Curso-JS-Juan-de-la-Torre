const producto = 'Monitor';
const precio = '50 USD';

//concatena dos strings
console.log(producto.concat(precio));

console.log(producto + "concatenado" +precio);

//emc 6
console.log(`${producto} nueva manera ${precio}`);